/**
 * Extension configuration — single source of truth.
 *
 * Change API_BASE here to point to your server.
 * All other files import from this module.
 */
export const API_BASE = "http://localhost:3000";
